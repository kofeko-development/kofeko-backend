# ─────────────────────────────────────────────
# REDIS (optional — session profile + API cache)
# ─────────────────────────────────────────────
# Local: redis://127.0.0.1:6379  |  Docker: docker run -d -p 6379:6379 redis:7-alpine
REDIS_URL=
CACHE_TTL_SECONDS=60
